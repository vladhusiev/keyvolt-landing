'use client'

import styles from './features.module.css'

export default function Features() {
	return (
		<section className={styles.features}>
			<div className={styles.container}>
				<h2 className={styles.title}>
					Рішення, яке підходить для усіх
				</h2>
			</div>
		</section>
	)
}
